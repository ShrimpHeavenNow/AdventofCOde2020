data = open('nums.txt')
numbers = []
for x in data:
    numbers.append(int(x))
for x in numbers:
    for y in numbers:
        for z in numbers:
            if x + y +z == 2020:
                solution = x*y*z
print(solution)
